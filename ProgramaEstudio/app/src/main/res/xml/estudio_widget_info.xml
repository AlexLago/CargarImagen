<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialKeyguardLayout="@layout/estudio_widget"
    android:initialLayout="@layout/estudio_widget"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:previewImage="@drawable/example_appwidget_preview"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="86400000"
    android:widgetCategory="home_screen"></appwidget-provider>
    <!--Aqui almacenamos los ajustes del widget como los bloques que ocupara, el layout que mostrará,
    tiempo de actualización, que se pueda expandir horizontal y/o verticalmente, etc-->